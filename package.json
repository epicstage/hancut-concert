{
  "name": "hancut-concert",
  "version": "1.0.0",
  "description": "한동훈과 한 컷 토크콘서트 신청 시스템",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name hancut-concert2",
    "deploy:prod": "wrangler pages deploy public --project-name hancut-concert2 --branch main",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0",
    "wrangler": "^3.0.0"
  }
}
